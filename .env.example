# Configuration for Wikipedia Research POC (Local LLM)

# LM Studio URL (optional - defaults to http://localhost:1234/v1)
# Make sure LM Studio is running with the server enabled
LM_STUDIO_URL=http://localhost:1234/v1

# Model name loaded in LM Studio (optional - defaults to qwen2.5-14b-instruct)
# Check LM Studio's "Local Server" tab to see the exact model identifier
LM_STUDIO_MODEL=qwen2.5-14b-instruct

# Note: No API keys needed! This runs 100% locally with LM Studio + Wikipedia
